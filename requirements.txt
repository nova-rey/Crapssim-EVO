# Optional runtime extras (install with: pip install -e .[runtime])
leap-ec>=1.0.0
pandas>=2.1
pyyaml>=6.0

# Development extras (install with: pip install -e .[dev])
pytest>=7.0
ruff>=0.5.0
black>=24.0.0
build>=1.2.1
